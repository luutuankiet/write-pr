---
title: "Demo PR: add widget cache"
root_path: "/tmp/widget-repo"
---

[[ 'TIP' | gh_callout('Synthetic demo PR — exercises every write-pr v0.2 filter and one snippet include. Real refactor PRs follow the same skeleton.') ]]

[% set tldr_rows = load_json('queries/q_synthetic.json').summary %]
[% include 'snippets/tldr_kv_table.j2' %]

## Changes

[[ 'code/widget_cache.py' | code_expand(root_path=evidence_dir, lines='1-6') ]]

## Validation

[[ load_json('queries/q_synthetic.json').validation | md_table ]]

## Before / After

[% set perf = load_json('queries/q_synthetic.json') %]
[[ perf.before | delta_table(perf.after) ]]

## Data Flow

```mermaid
flowchart LR
    A["Request"] --> B["WidgetCache"]
    B -->|miss| C["DB"]
    B -->|hit| D["Response"]
    C --> D
```

## Appendix

[[ load_json('queries/q_synthetic.json') | json_pretty | fold('Raw query result (JSON)') ]]

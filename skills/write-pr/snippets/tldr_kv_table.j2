[# tldr_kv_table.j2 — top-of-PR 2-col metrics summary.
   5/7 surveyed PRs led with this pattern.

   Variables expected in scope:
     tldr_rows: array of {metric: str, value: str}

   Inline / copy-paste form:
     ## TL;DR

     [[ load_json('queries/q_summary.json').rows | md_table ]]
#]

## TL;DR

[[ tldr_rows | md_table ]]

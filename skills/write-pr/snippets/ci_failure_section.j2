[# ci_failure_section.j2 — dbt CI run failure attribution.
   Use when CI fails on a PR but the failure is NOT in models this PR touched,
   or to clearly attribute when it IS.

   Variables expected in scope:
     ci_run_url_md:      pre-formatted markdown link to the CI run
     run_results_path:   path to dbt's run_results.json, relative to --evidence dir
     our_models:         list of substrings/unique_ids that count as PR-touched models

   Output structure:
     - status counts table (success / error / skipped / fail / warn)
     - elapsed time
     - errors table (only when errors > 0)
     - our-models breakdown (only when our_models has matches)
#]

## CI Failure Attribution

**Run:** [[ ci_run_url_md ]]

[[ load_json(run_results_path) | ci_summary(our_models) ]]

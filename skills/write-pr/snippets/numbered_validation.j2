[# numbered_validation.j2 — numbered validation sub-section.
   5/7 surveyed PRs used `### N. <Title>` style nested validation sub-sections.

   Variables expected in scope:
     n:        section number (int)
     title:    section title (e.g. 'Per-product SUM parity')
     body_md:  rendered markdown body (string; may include tables, code blocks, <details>)

   Wrap the body in <details> for scannability. Note: GitHub callouts do not
   render styled inside <details> — keep [!TIP] / [!NOTE] outside.
#]

### [[ n ]]. [[ title ]]

[[ body_md ]]
